import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-nycu-bme-pastexam',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
